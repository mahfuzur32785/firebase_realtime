import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  static const red = Color(0xFFE30909);
  static const orange = Colors.deepOrange;
  static const black = Color(0xEE000000);
  static const white = Color(0xFFFFFFFF);
  static Color grey = Colors.grey;
  static Color customGrey = Colors.grey.shade300;

  // static const secondaryColor = Color(0xFF00BCEF);
  static const primaryColor = Color(0xFF212d6e);
  // static const primaryColor = Color(0xFF1d395d);
  static const secondaryColor = Color(0xFF00BCEF);

  static const textBgColor = Color(0xFFD1B896);
  static const textColor = Colors.deepOrange;
  static const viewDetailsColor = Color(0xFF3D868D);

  static const liveStatus = Color(0xFF1ABC9C);
  static const upComing = Color(0xFF845ADF);
  static const complete = Color(0xFF388229);
  static const pending = Color(0xFFFEA920);







}
